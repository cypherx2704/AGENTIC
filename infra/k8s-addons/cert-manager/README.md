# k8s-addons/cert-manager — cert-manager (Component 9)

Terraform-managed Helm release for cert-manager plus the `letsencrypt-prod`
`ClusterIssuer`.

- **Chart:** `cert-manager/cert-manager` (`var.chart_version`, default `v1.15.1`)
- **Namespace:** `cert-manager`
- **ClusterIssuer:** `letsencrypt-prod` (ACME / Let's Encrypt production)

## Scope — internal dashboards ONLY (do not widen)

cert-manager manages TLS for **developer-facing dashboard ingresses only**
(e.g. `grafana.<env>.cypherx.ai`, `argocd.<env>.cypherx.ai` behind the
internal / VPN-only ingress). The HTTP-01 solver routes through the internal
ingress class (`var.solver_ingress_class`, default `kong`), never the public
ACM-terminated ALB.

| Cert domain | Managed by | NOT this module |
|-------------|-----------|-----------------|
| Public ALB certs (`*.<env>.cypherx.ai`) | **AWS ACM** (auto-renew) | ✔ |
| Mesh / sidecar certs | **Istio CA** | ✔ |
| Internal dashboard ingress certs | **cert-manager** (this module) | — |

## Inputs

| Variable | Default | Notes |
|----------|---------|-------|
| `env` | — | `dev` \| `staging` \| `prod` |
| `chart_version` | `v1.15.1` | Pinned |
| `namespace` | `cert-manager` | |
| `acme_email` | — | ACME account contact (operational, not a secret) |
| `letsencrypt_acme_server` | LE prod directory | |
| `solver_ingress_class` | `kong` | Internal ingress class for HTTP-01 |

## Secrets

None hardcoded. The ACME account private key is generated by cert-manager and
stored in the `letsencrypt-prod-account-key` Secret.
