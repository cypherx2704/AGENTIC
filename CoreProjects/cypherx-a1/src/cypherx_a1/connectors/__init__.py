"""Source connectors. Each connector implements the connector SPI (:mod:`base`) and
normalizes one source (GitHub first) into the canonical model. Adding a market tool = one
SPI class + one registry row, zero core changes."""
