# Intentionally empty at Phase 1 end (Component 19).
# ArgoCD root app `cypherx-platform-dev` (apps/dev-apps.yaml) watches this dir.
# Service teams add their app here in their deploy phase:
#   envs/dev/<namespace>/<service>/  (e.g. envs/dev/shared-core/auth/)
# Each child app added here is auto-created by ArgoCD and AUTO-SYNCS (dev policy).
