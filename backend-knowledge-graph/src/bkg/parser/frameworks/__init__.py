"""Framework adapters: each turns framework-specific structures in a neutral
``SemanticModel`` into framework-independent ``FrameworkFacts``. FastAPI first, then
Flask, then (post engine-migration) Express, Spring, …"""
