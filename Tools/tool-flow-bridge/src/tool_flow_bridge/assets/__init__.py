"""Packaged, versioned assets shipped with the bridge (loaded via importlib.resources).

Currently holds ``web_search_flow.json`` — the importable Node-RED flow behind the PUBLIC
``web_search`` flow-tool (the drop-in replacement for the bespoke tool-web-search service).
"""
